Dane generowane w pliku dla części wizytówek prywatnych i dla części odpowieadającej za dane w wizytówkach firmowych generowane są "za każdym razem" czyli bedą losowo różne.
Na drugiej gałęzi umieszczony plik w którym to naprawiono (chyba ? :) )
