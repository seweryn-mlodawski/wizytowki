Dane generowane w pliku dla części wuzytówek prywatnych i dla części odpowieadającej za dane w wizytówkach firmowych generowane są "za każdym razem" czyli bedą losowo różne.
Na drugiej gałęzi umieszczony plik w któym to naprawiono (chyba ? :) )
